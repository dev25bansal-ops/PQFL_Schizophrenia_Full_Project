# @package _global_
# FedAvg baseline experiment

defaults:
  - override /federated: fedavg

federated:
  strategy: "fedavg"
  n_rounds: 50
  n_local_epochs: 5

model:
  n_base_layers: 4
  n_personal_layers: 0
